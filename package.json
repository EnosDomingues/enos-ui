{
  "name": "enos-ui",
  "private": "true",
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "engines": {
    "node": ">=20.15.1"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
