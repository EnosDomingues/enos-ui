import { Meta } from '@storybook/addon-docs/blocks'
import { TokensGrid } from '../../components/TokensGrid'
import { space } from '@enos-ui/tokens'

<Meta title="Tokens/Spaces" />

# Space Guidelines

The Enos UI space guidelines define a fundamental aspect of our design system, meticulously designed to ensure consistency, enhance readability, and guide user interaction through structured spacing. Below are the space tokens, their applications, and a rationale for their use.

<TokensGrid tokens={space} hasRemValue/>
