import { Meta } from '@storybook/addon-docs/blocks'
import { TokensGrid } from '../../components/TokensGrid'
import { fonts } from '@enos-ui/tokens'

<Meta title="Tokens/Fonts" />

# Font Families

Our typography system is foundational to user experience, influencing readability, tone, and visual identity. Below, we detail the font stack we employ, crafted to ensure versatility across different platforms and devices while maintaining our brand's typographic consistency.

<TokensGrid tokens={fonts}/>
