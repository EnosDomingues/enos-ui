import { Meta } from '@storybook/addon-docs/blocks'
import { TokensGrid } from '../../components/TokensGrid'
import { fontWeights } from '@enos-ui/tokens'

<Meta title="Tokens/Font Weights" />

# Font Weights

Typography plays a crucial role in user interface design, with font weights being a key aspect in establishing visual hierarchy and conveying tone. Below is a detailed overview of the font weights used in our UI, designed to enhance readability and ensure a consistent visual language throughout various applications.

<TokensGrid tokens={fontWeights}/>
